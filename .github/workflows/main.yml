name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      TELEGRAM_TOKEN: 6202443309:AAFNCM4w44Bev7kMSAvlcSarhSUqK5a1WpQ
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build Docker image
        run: docker-compose --env-file config/config.env up --build -d

      - name: Set webhook URL
        run: |
         curl -F "url=https://github.com/luv06/LuvGPT" https://api.telegram.org/bo6202443309:AAFNCM4w44Bev7kMSAvlcSarhSUqK5a1WpQ/setWebhook
